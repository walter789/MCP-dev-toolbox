# Absolute path to the codebase you want to analyze.
# All file and git tools operate within this directory.
WORKSPACE_ROOT=/path/to/your/project

# Comma-separated directory names to exclude from file searches.
EXCLUDED_DIRS=.git,__pycache__,.venv,venv,node_modules,.mypy_cache,.pytest_cache
